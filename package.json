{
  "name": "live-polling-system",
  "version": "1.0.0",
  "description": "Resilient Live Polling System with Teacher and Student personas",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [
    "polling",
    "socket.io",
    "react",
    "nodejs"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "axios": "^1.13.2",
    "concurrently": "^8.2.2"
  }
}
